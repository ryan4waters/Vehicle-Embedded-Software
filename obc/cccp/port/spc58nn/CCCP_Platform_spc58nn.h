#ifndef CCCP_PLATFORM_SPC58NN_H
#define CCCP_PLATFORM_SPC58NN_H

#include "CCCP_Platform.h"

/*
 * Timer input capture, ADC and GPIO/IRQ are isolated here.
 * Whether the project uses MCAL ICU/ADC/PORT or low-level drivers
 * is deliberately not visible to common/.
 */

#endif
